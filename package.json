{
  "name": "jamesmunro.github.io",
  "private": true,
  "scripts": {
    "build": "eleventy && npm run build:jupyter",
    "build:jupyter": "jupyter lite build --output-dir=_site/tools/jupyter-lite",
    "dev": "eleventy --serve",
    "test": "node --test tools/**/*.test.{js,mjs}",
    "lint": "node --check tools/jwt-reader/jwt-reader.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "jsdom": "^27.4.0"
  },
  "dependencies": {
    "chart.js": "^4.4.0",
    "color": "^4.2.3",
    "proj4": "^2.11.0",
    "pyodide": "^0.29.1",
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.8.0"
  }
}
